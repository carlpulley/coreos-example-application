# Environment variables:
#
#   ETCD_URL - URL pointing to our compute nodes etcd service

etcd {
  akka = "akka.cluster.nodes"
  key {
    timeout = 20 s
    refresh = 10 s
    failure.count = 3
  }
  url = ${?ETCD_URL}
}
